<h1>hi from add users</h1>

<%= form_with model: @competition, url: update_users_competition_path(@competition), method: :post do |field| %>
  <%= field.collection_check_boxes(:user_ids, @eligible_users, :id, :email, {include_hidden: false}) %>
  <%= field.submit "Add Users"%>
<% end %>
